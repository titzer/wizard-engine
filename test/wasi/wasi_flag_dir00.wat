;; A test case to confirm the behavior of directory related flags.
;; Therefore, wasm file itself is almost empty.

;; dir00: Normal Case - wizeng should accept directories using --dir flag
(module
  (func $main (export "main")
  )
)
