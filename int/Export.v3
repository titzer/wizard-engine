export "fast:foo" def foo(x: int, y: int) -> int {
	var val = y;
	for (i < x) {
		val += i * y;
	}
	return val;
}

export "main" def main() -> int {
	System.puts(Strings.format1("%d\n", foo(11, 2)));
	return 0;
}
