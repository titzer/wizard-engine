#!/bin/bash

while [[ "$1" =~ "-trace-calls" ]]; do
  V3C_OPTS="${V3C_OPTS} $1"
  shift
done

W=$WIZENG_LOC
v3c -run -fp $V3C_OPTS $W/src/wave.main.v3 $W/src/engine/*.v3 $W/src/util/*.v3 $W/src/wave/*.v3 $@
