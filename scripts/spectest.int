#!/bin/bash

while [[ "$1" =~ "-trace-calls"  || "$1" =~ "-fatal-calls"  ]]; do
  V3C_OPTS="${V3C_OPTS} $1"
  shift
done

W=$WIZENG_LOC
V=$VIRGIL_LOC
v3c -run -fp $V3C_OPTS $W/test/spectest.main.v3 $W/src/engine/*.v3 $W/src/engine/v3/*.v3 $W/src/util/*.v3 $W/test/spectest/*.v3 $V/lib/util/*.v3 $@
